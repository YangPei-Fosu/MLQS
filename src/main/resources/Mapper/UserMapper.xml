<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.mlqs.user.db.UserMapper">
    <!-- 根据会话ID查询（返回id、message、时间） -->
    <select id="selectBySessionId" resultType="io.mlqs.user.db.UserEntity">

    </select>

    <!-- 查询所有有效数据 -->
    <select id="selectAllValid" resultType="io.mlqs.memory.db.MemoryEntity">

    </select>

</mapper>