<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.mlqs.memory.db.MemoryMapper">
    <!-- 根据会话ID查询（返回id、message、时间） -->
    <select id="selectBySessionId" resultType="io.mlqs.memory.db.MemoryEntity">
        SELECT session_id AS sessionId,
               memory,
               create_time AS createTime,
               update_time AS updateTime,
               delete_flag AS deleteFlag
        FROM bas_memory
        WHERE session_id = #{sessionId}
          AND delete_flag = 0
    </select>

    <!-- 查询所有有效数据 -->
    <select id="selectAllValid" resultType="io.mlqs.memory.db.MemoryEntity">
        SELECT session_id AS sessionId,
               memory,
               create_time AS createTime,
               update_time AS updateTime,
               delete_flag AS deleteFlag
        FROM bas_memory
        WHERE delete_flag = 0
        ORDER BY update_time DESC
    </select>

</mapper>